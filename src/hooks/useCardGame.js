import solutions from "../data/solutions.json" assert { type: "json" };

export function useCardGame({index, interactions}) {

    // shuffle
    
    // interactions: count max 3

    // show form

    // show all solutions
}